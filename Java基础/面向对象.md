## 什么是面向对象
对象：把相关的数据和方法组织为一个整体来看待
面向对象：利用对象进行软件开发
```
面向对象三大特征：封装，继承，多态
封装：比如javabean类
继承：
```
## 类和对象
用一个类去描述对象
```
Dog d = new Dog()；
```
new:创建一个大空间
Dog:管理狗狗的信息
d:大空间的名字

利用面向对象思想表示狗狗（属性：名字，年龄，体重，毛发颜色）
```
package com.hytc.edu.Util;  
  
public class Dog {  
    String name;  
    int age;  
    String weight;  
    String color;  
}

package com.hytc.edu.Util;  
  
public class zhu{  
    public static void main(String[] args) {  
        Dog dog = new Dog();  
        dog.name = "小黑";  
        dog.age = 12;  
        dog.color = "黑色";  
        dog.weight = "10.5kg";  
        System.out.println("狗的名字是：" + dog.name);  
        System.out.println("狗的年龄是：" + dog.age);  
        System.out.println("狗的体重是：" + dog.weight);  
        System.out.println("狗的颜色是：" + dog.color);  
    }  
}
```
## 面向对象的小细节
描述一类事物的类叫Javabean类
带有main方法的类叫测试类
Javabean类中可以写属性和行为

### 练习:
1 面向对象思想表示老师
属性:姓名、年龄
行为:授课、吃饭、睡觉
```
package com.hytc.edu.leet;  
  
public class teacher {  
    String name;  
    int age;  
    public void eat(){  
        System.out.println("老师吃饭");  
    }  
    public void sleep(){  
        System.out.println("老师睡觉");  
    }  
}

package com.hytc.edu.leet;  
  
public class main {  
    public static void main(String[] args) {  
        teacher t = new teacher();  
        t.name = "张三";  
        t.age = 30;  
        t.eat();  
        t.sleep();  
    }  
}
```

## 面向对象中的数据安全问题
private关键字：
是一个权限修饰符,可以修饰成员变量和成员方法。
特点:一旦被private修饰,只能在本类中才能访问,外界无法访问。
public:公共的
get/set方法：
set方法是用来赋值的，可以提前判断数据是否合理从而选择赋值
get方法是获取数据
### 怎么解决面向对象中的数据安全问题：
一是私有化成员变量
二是私有化成员变量要提供对应的get/set方法

```
面向对象练习:小狗
小狗的属性:姓名、年龄。
行为:吃骨头
实现以下需求
要求1:小狗的年龄:0~15岁
要求2:打印小狗所有的信息:小白,2岁
要求3:调用吃骨头的方法打印效果:2岁的小白,正在吃骨头
```

```
package com.hytc.edu.Util;  
  
public class Dog {  
    private String name;  
    private int age;  
    public void eat(){  
        System.out.println(age+"岁的"+name+"吃骨头");  
    }  
  
    public String getName() {  
        return name;  
    }  
  
    public void setName(String name) {  
        this.name = name;  
    }  
  
    public int getAge() {  
        return age;  
    }  
  
    public void setAge(int age) {  
        if(age>15||age<0){  
            System.out.println("年龄不合法");  
            return;  
        }  
        this.age = age;  
    }  
}

package com.hytc.edu.Util;  
  
public class zhu{  
    public static void main(String[] args) {  
        Dog dog = new Dog();  
        dog.setName("小黑");  
        dog.setAge(12);  
        dog.eat();  
    }  
}
```

## this关键字
用于区分成员变量和局部变量
成员变量：定义在方法外面的变量
局部变量：在方法里面的变量包括形参
由于形参可能和成员变量重名，想要准确的使用成员变量就要只用this关键字
变量的就近原则：先找局部变量再找成员变量

## 构造方法
构造方法也叫构造器，构造函数
作用：在创建对象的时候给成员变量进行初始化的
特点:
方法名与类名相同,大小写也要一致
没有返回值类型,连void都没有
没有具体的返回值(不能由retrun带回结果数据)
执行时机:
创建对象的时候由虚拟机调用,不能手动调用构造方法
每创建一次对象,就会调用一次构造方法
```
package com.hytc.edu.Util;  
  
public class Dog {  
    private String name;  
    private int age;  
    public Dog() {  
        System.out.println("无参构造方法被调用");  
    }  
    public Dog(String name, int age) {  
        this.name = name;  
        this.age = age;  
        System.out.println("有参构造方法被调用");  
    }  
    public void eat(){  
        System.out.println(age+"岁的"+name+"吃骨头");  
    }  
  
    public String getName() {  
        return name;  
    }  
  
    public void setName(String name) {  
        this.name = name;  
    }  
  
    public int getAge() {  
        return age;  
    }  
  
    public void setAge(int age) {  
        if(age>15||age<0){  
            System.out.println("年龄不合法");  
            return;  
        }  
        this.age = age;  
    }  
}


package com.hytc.edu.Util;  
  
public class zhu{  
    public static void main(String[] args) {  
        Dog dog1 = new Dog();  
        Dog dog = new Dog("小黑", 12);  
    }  
}
```
## 构造方法的注意事项
如果没有定义构造方法,系统将给出一个默认的无参数构造方法(因为创建对象的时候要用到)
如果自己写了任意构造方法,系统将不再提供默认的构造方法
带参构造方法和无参数构造方法,两者方法名相同,但是参数不同,这叫做构造方法的重载

### 习惯:无论是否使用,都手动书写无参数构造方法,和带全部参数的构造方法