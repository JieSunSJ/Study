创建第一个java程序
使用命令行
编译程序
```
javac HelloWorld.java
```
会生成一个HelloWorld.class文件
运行程序
```
java HelloWorld
```
输出

## java的跨平台原理
一次编译，处处可用


## IDEA创建Java项目的代码结构
Project
1 创建工程 new Project(空工程)

Module
2 创建模块 new Module

Package
3 创建包 new Package

Class
4 创建类

## 主函数
快捷 psvm

快捷输出 “ ”.sout

用注释提示ai写代码

## IDEA中的其他操作
打开工程，直接在idea里面open,工程文件夹下表有黑点
关闭工程直接关

修改模块
重命名，模块和文件夹
导入模块
从file里面导入模块
![[Pasted image 20260528221716.png]]
然后配置jdk
删除模块
先移除后删除
## 方法
功能的最小单位是一个方法
![[Pasted image 20260528222640.png]]
### 方法详解
方法是一种用于执行特定任务或操作的代码块,代表一个功能,它可以接收数据进行处理,并返回一个处理后的结果。
方法是否需要接收数据处理?
可以不接收数据处理
方法是否需要返回数据?
可以不返回数据，void:无参数返回值
### 方法的一些注意事项
1.方法可以重载
一个类中,出现==多个方法的名称相同==,但是它们的==形参列表是不同的==,那么这些方法就称为==方法重载==了。
```
public static void a(int a,int b) {  
}  
public static void a(int a,double b) {  
}  
public static void a(double a,int b) {  
}  
public static void a(double a,double b) {  
}  
public static void a(String a,int b,int c) {  
}
```
在无返回值的方法中使用return; 可以提前结束方法。
## 注释
```
//单行
```
```
/*
多行注释
*/
```
```
/**
文档注释
*/
```
## 字面量

| 常用数据    | 生活中的写法      | 程序中         | 说明                       |
| ------- | ----------- | ----------- | ------------------------ |
| 整数      | 666，-88     | 666，-88     | 写法一致                     |
| 小数      | 13.14，-5.21 | 13.14，-5.21 | 写法一致                     |
| 字符      | A,0,我       | ‘A’，‘0’，‘我’ | 程序中必须使用单引号,有且仅能一个字符      |
| 字符串     | 你好          | “你好”        | 程序中必须使用双引号,内容可有可无        |
| 布尔值     | 真，假         | true,false  | 只有两个值:true:代表真,false:代表假 |
| 空值      | -           | 值是：null     | 一个特殊的值,空值(后面会讲解作用,暂时不管)  |
| 特殊字符字面量 | -           | \t \n       |                          |
## 变量详解
```
数据类型 变量名称 = 数据；
```
![[Pasted image 20260528223929.png]]

### 变量里数据的存储原理
二进制01
数据在计算机的最小存储单元：字节（byte，简称B），一个字节占八个比特位（bit，简称b），因此1B=8b

字符数据在计算机如何存储
ASCII码

图片数据是如何存储的
图片就是无数个像素点组成的 三原色
每个像素点的数据:用0~255 * 255 * 255表示其颜色

声音数据是如何存储的
波形图

## 进制
二进制转十进制
8421

为了便于观察二进制，推出了八进制和十六进制
二进制三位一组构成八进制
二进制四位一组构成十六进制

Java程序中支持书写二进制、八进制、十六进制的数据,分别需要以==OB==或者==0b==、==0==、==0X==或者==0x==开头。

## 数据类型
![[Pasted image 20260529141619.png]]
```
public static void main(String[] args) {  
    // 基本数据类型  
    byte b = 127;  
    short s = 32767;  
    int i = 1000;  
    long l = 10000L;  
  
    float f = 3.14f;  
    double d = 3.14159;  
  
    char c = 'A';  
    boolean bool = true;  
  
    // 引用数据类型 (示例)  
    String str = "Hello, Java!";  
  
    System.out.println("byte: " + b);  
    System.out.println("short: " + s);  
    System.out.println("int: " + i);  
    System.out.println("long: " + l);  
    System.out.println("float: " + f);  
    System.out.println("double: " + d);  
    System.out.println("char: " + c);  
    System.out.println("boolean: " + bool);  
    System.out.println("String: " + str);  
}
```
## 关键字，标识符
### 关键字（Keyword）
关键字是由Java语言预定义的、具有特殊意义的保留字。这些词不能用作变量名、方法名、类名等标识符。Java的关键字都是小写的。
```
abstract：用于声明抽象类或抽象方法
boolean：布尔类型
break：用于跳出循环或switch语句
byte：字节类型
case：switch语句中的分支
catch：异常处理中的捕获块
char：字符类型
class：声明类
const：保留字（不使用）
continue：跳过当前循环的剩余部分
default：switch语句中的默认分支
do：do-while循环的开始
double：双精度浮点类型
else：if语句的else分支
enum：枚举类型
extends：表示继承关系
final：表示最终的，不可修改的
finally：异常处理中的最终执行块
float：单精度浮点类型
for：for循环
goto：保留字（不使用）
if：条件语句
implements：实现接口
import：导入包
instanceof：检查对象是否属于某个类
int：整数类型
interface：声明接口
long：长整数类型
native：本地方法
new：创建新对象
package：声明包
private：私有访问修饰符
protected：受保护的访问修饰符
public：公共访问修饰符
return：返回值
short：短整数类型
static：静态成员
strictfp：严格浮点运算
super：父类引用
switch：多路分支语句
synchronized：同步关键字
this：当前对象引用
throw：抛出异常
throws：声明异常
transient：临时变量
try：尝试执行代码块
void：无返回值
volatile：易变变量
while：while循环
```
### 标识符（Identifier）
标识符是用来给变量、方法、类、接口等命名的符号。标识符必须遵循以下规则：
标识符可以由字母、数字、下划线_和美元符号（$）组成。
标识符==不能以数字==开头。
标识符不能是Java的关键字。
标识符区分大小写。
标识符长度没有限制。

==变量名==:建议首字母小写,满足“==驼峰模式==”

==类名==:建议首字母大写,满足“驼峰模式”
## 类型转换
### 自动类型转换(小转大)
什么是自动类型转换,为什么要进行自动类型转换?
类型范围小的变量,可以直接赋值给类型范围大的变量。
![[Pasted image 20260529145955.png|249]]
在表达式中,小范围类型的变量,会自动转换成表达式中较大范围的类型,再参与运算。
表达式的最终结果类型由表达式中的==最高类型==决定。
在表达式中,byte、short、char 是直接转换成int类型参与运算的。
### 强制类型转换(大转小)
```
int i = 10;
byte j = (byte) i;
```
强制类型转换可能造成数据(丢失)溢出;浮点型强转成整型,直接丢掉小数部分,保留整数部分返回
![[Pasted image 20260529150355.png]]
## 输入输出
快捷键sout
输入:程序读取用户键盘输入的数据。通过Java提供的Scanner程序来实现
==Scanner==是Java提供好的API,程序员可以直接调用
==API==(Application Programming Interface:应用程序编程接口)

```
Scanner sc = new Scanner(System.in); //让用户在这一行暂停，直到用户按了回车键之后，把名字交给变量name 
System.out.println("请输入你的名字：");  
String name = sc.next();  
System.out.println("Hello World, " + name);
```
## 运算符
![[Pasted image 20260529153559.png|268]]
### +符号在Java中的特殊用途
“+” 符号在有些情况下可以做连接符。
“+”符号与字符串运算的时候是用作连接符的,其结果依然是一个字符串。
```
String a = "aaaa";  
int b=1;  
System.out.println(b+a);//1aaa  
System.out.println(a + b);//aaa1  
System.out.println(b + 'a' + a);//98aaaa  
System.out.println(a + b + 'a');//aaaa1a
```
### 自增自减运算符
自增:++
放在某个变量前面或者后面,对变量自身的值加1
自减 :--
放在某个变量前面或者后面,对变量自身的值减1
都不能操作字面量（2++）

#### 自增自减的使用注意事项
++、 -- 如果在变量前后单独使用是没有区别的。
![[Pasted image 20260529154414.png]]
### 赋值运算符
![[Pasted image 20260529154617.png]]
### 关系运算符，三元运算符
![[Pasted image 20260529155942.png]]
![[Pasted image 20260529160017.png]]
### 逻辑运算符
![[Pasted image 20260529160108.png]]
![[Pasted image 20260529160159.png]]

综合小案例
健康计算器
开发一个简单的健康计算器应用程序,它可以接受用户的输入(如年龄、性别、体重、身高),并计算出用户的BMI(身体质量指数)和基础代谢率(BMR)

## 程序流程控制
程序的三种执行顺序
顺序结构
分支结构
循环结构

### 分支结构
#### if分支结构
根据条件的真或假,来决定执行某段代码。
```
if(){}else if(){}else{}
```
#### switch分支结构
是通过比较值是否相等,来决定执行哪条分支。
![[Pasted image 20260531201444.png]]
switch分支的执行流程
1 先执行表达式的值,再拿着这个值去与case后的值进行匹配。
2 与哪个case后的值匹配为true就执行哪个case块的代码,遇到break就跳出switch分支。
3 如果全部case后的值与之匹配都是false,则执行default块的代码。

if、switch的比较:各自适合什么业务场景?
if在功能上远远强大于switch。
当前条件是区间的时候,建议使用if分支结构来实现
当条件是与==一个一个的值==比较的时候,建议用==switch更合适==(格式良好性能较好代码优雅)。

switch注意事项
1 表达式类型只能是byte、short、int、char,JDK5开始支持枚举,JDK7开始支持String、不支持double、float、long。
2 case给出的值不允许重复,且只能是字面量,不能是变量。
3 正常使用switch的时候,不要忘记写break,否则会出现穿透现象。
因为java中存小数
## 循环结构
### for
重复执行一段代码
案例
计算1到5的和
```
for (int i = 1; i <= n; i++) {
sum += i;
}
```
### while
![[Pasted image 20260531204230.png]]
不知到循环了多少次可以用while循环
### do-while
![[Pasted image 20260531205230.png]]
## 随机函数
random()用法
```
Random random = new Random();
x = random.nextInt()；
生成指定范围的整数（最常用）
范围 [0, N)：nextInt(N) 取不到N。
范围 [min, max]（包含两端）：
```
